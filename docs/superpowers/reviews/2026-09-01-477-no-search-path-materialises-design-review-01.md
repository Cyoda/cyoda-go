# Review 01 — #477 "No search path materialises a model" design

Reviewed against `134bcaa` (worktree HEAD `09628c6`), SPI `f6863ae`, commercial checkout `494a334` (pinned at SPI v0.8.3). Every claim below was checked in code, not in the research doc. "## 10. Accepted trade-offs" respected: nothing listed there is re-raised; one false *supporting* claim inside §10 is corrected under Q2 because it is a fact, not a trade-off.

## 1. Seam

[MINOR] Owning packages are named and each keeps one responsibility per ARCHITECTURE §1: `internal/domain/search` ("Sync + async search, predicate evaluation") keeps the routing and the bounded drain (§2); the SPI root keeps the contract (§3); each plugin keeps its own storage mechanics (§4); `internal/match` is untouched (§11). No decorator wraps `spi.EntityStore` outside tests (`internal/observability` decorates only the TM, `internal/cluster/modelcache` only `ModelStore`), so the "compiler-driven removal" in §8 PR 3 has no hidden seam. No finding. — §2–§4 — none.
[MINOR] §3.2 deletes the `Iterable` interface but does not say where `Iterator` and `IterateOptions` (both in `iterable.go` today) live afterwards. — §3.2 — state the file (keep `iterable.go` for the two types, or fold into `persistence.go` beside `EntityStore`).
[MINOR] §4.1 is a change to the sqlite *transaction manager* (`txmanager.go`), filed under "Plugins — memory, sqlite, postgres" beside entity-store items. It is the right seam, but say explicitly that entity-store code never reads `lastSubmitTime` and the gate lives only in `Begin`. — §4.1 — add one sentence naming the seam.

## 2. Invariants

[MINOR] Touched: `correctness-over-availability.md` ("never with a substituted or fallback value standing in for the correct one") — complied with, and it is the stated reason the `GetPage` rung is rejected in §2.2; `direct-search-bounded-or-fail.md` invariants 1–3 — complied with (the `Limit + 1`-th yield raises, no offset); `tx-aware-search.md` invariant 1 ("with no full-model materialisation as the mechanism") — this design is what makes it true for memory/sqlite in-tx reads, and invariant 3 ("records only the returned entities … not the whole candidate set") — see Q3 first finding; `path-grammar.md` §11 ("The classification applies on every route to the backend") — §2.1 classifies `translateErr` on the new rung, complied with; `operator-semantics.md` §7 pushdown narrows, residual decides — the rung's residual runs inside `Next()`, complied with; CLAUDE.md Gate 4 (COMPATIBILITY on pin bump — listed in §8) and Gate 7 (parity-doc edits — listed in §8); `multi-node-primary.md` — no cluster-visible change (in-tx search stays co-located per tx-aware-search inv. 4). No violation found. — §2, §8 — none.
[MINOR] §10 supports the sqlite Begin gate with "the memory plugin already pays it". It does not: memory `Begin` (`plugins/memory/txmanager.go:272-276`) takes only `m.mu`, captures `SnapshotTime` and `txSnapshotSeq` atomically, and never waits on a commit; memory's FCW is sequence-based and its visibility is atomic under `entityMu`, so the window §4.1 describes cannot open there. The trade-off stands; the comparison is false. — §10, §4.1 — replace with the true reason: sqlite's row visibility on `readDB` is not atomic with its `lastSubmitTime` bump, and only serialising `Begin` behind the flush makes "SnapshotTime ≥ submitTime" imply "rows visible".
[MINOR] `direct-search-bounded-or-fail.md` needs more than "invariants 1–2": the Behaviour bullet "`limit <= 0` means unbounded. A storage plugin must not substitute a default", the SPI-level bullet "`Limit <= 0` MUST be treated as unbounded" (already contradicted by `searcher.go:17-22` at `f6863ae`), and the "Known commercial-backend gap" paragraph (re-defaulting a non-positive limit "is a divergence from rule 2") all become wrong or inverted once §2.1 makes `Limit <= 0` an engine error. — §8 docs — list all four passages.
[MINOR] `path-grammar.md` §11 says classification applies to "the bounded search call and the unbounded streaming drain alike. Classifying one route only makes the answer depend on whether the request carried a positive `limit`" — that is the `Limit <= 0` route §2.1 deletes; the file is not in §8's doc list. — §8 docs — add `path-grammar.md` §11 and reword to "the `Searcher` call and the `Iterate` rung alike".
[MINOR] `tx-aware-search.md` names `GetAllAsAt` in the PIT carve-out paragraph ("`GetAsAt`, `GetAllAsAt`, `GetPage(asAt)` and `Iterate(PointInTime)`") and in invariant 1's list, beyond the `:14-16` fallback sentence §8 cites. — §8 docs — extend the edit to every `GetAllAsAt` mention in that file.

## 3. Completeness

[MAJOR] §4.4 says "each yield records" but not *which* yield: `MergeOrdered`'s output (every merged committed-and-buffered row of the model, pre-residual — what `getAllTx` records today at `plugins/sqlite/entity_store.go:600-628`) or `Iterator.Next() == true` (post-residual, matches only — what `IterateOptions.TrackingRead` promises: "records the entities this iteration yields"). It also does not say whether the in-tx committed cursor carries `planFor(filter).where` pushdown the way `searchTxOverlay` does (`plugins/sqlite/searcher.go:235+`) or streams the whole model with the residual in Go. The two choices decide the sqlite in-tx read-set contract, and §4.5 leans on §4.4 ("the pattern 4.4 introduces"). — §4.4 — state: the committed cursor applies `plan.where` and `plan.preparedPostFilter` exactly as `searchTxOverlay`; the buffered adds are filtered by the same `PreparedFilter`; recording happens only when `Next()` returns true; add a plugin test where a filter excludes some committed rows and asserts those ids are absent from `tx.ReadSet` (spitest `TrackingRead/Gating` checks a single yielded id and would not catch pre-residual recording).
[MAJOR] The §6 coverage matrix drops behaviours the deleted fallback pinned. `handler_timeout_test.go:162` (`TestSearch_FallbackLoop_PreExpiredCtx_ReturnsDeadlineExceeded`, reached through `nonSearcherEntityStore`) pins ctx-expiry on this exact rung and §5 lists 408 as a code that must still hold, yet there is no cancellation/mid-drain-timeout cell for the `Iterate` rung; `fallback_typed_test.go:70,117` (type-directed matching, fail-closed on schema-load failure) and `classify_store_query_error_test.go:345` are likewise unmentioned. The research (§8) lists six tests; the spec names the fate of none of them. — §6, §8 — add rows "Iterate rung: pre-expired ctx and mid-drain cancellation → `DeadlineExceeded` chain (408 at the handler)" and "Iterate rung: type-directed comparison via the plugin kernel (carry `fallback_typed_test.go`'s assertions)"; add a §8 bullet listing each of the six tests as rewritten or deleted with its replacement.
[MINOR] §4.4's per-yield check names only `RolledBack`. `Commit` (`txmanager.go:311-315`) takes `tx.OpMu.Lock()` between two per-yield `RLock`s and sets `tx.Closed`; a same-tx iterator still open afterwards would keep recording into a committed tx's `ReadSet`. The SPI forbids *mutating* the tx while an iterator is open; committing is not listed. — §4.4 — check `Closed` as well and end iteration with `ErrTxAlreadyCommitted` (or record nothing), and add it to the deadlock test.
[MINOR] Moving in-tx `Iterate`/`GetPage`/`Count` onto `readDB` adds long-lived cursor holders to a pool sized by `defaultReaderPoolSize` (`store_factory.go:194`). Verified that in-tx `Get`/`getSnapshot` stays on `s.db` (`entity_store.go:466-490`) and `searchTxOverlay` on `s.db`, so a cursor's consumer never needs a second `readDB` connection to progress; the doc should say so, and decide whether `searchTxOverlay` also moves to `readDB` (Gate 6: one in-tx read path, not two). — §4.4 — one sentence on the pool assumption and one decision on `searchTxOverlay`.
[MINOR] The §1 table has no postgres row and §4 says nothing about postgres beyond §4.3's `Count`. Verified: postgres in-tx `DeleteAll` selects `entity_id` only (`plugins/postgres/entity_store.go:401`), `GetPage`/`Iterate` run on the tx connection. — §1 — add "postgres: audited, no materialising path" so an implementer does not re-audit.
[MINOR] §4.3's `committedTotal(snapshot)` and the `IN` term must use `searchSnapshotBase`'s latest-version-at-`submit_time <= snapshot` with `change_type != 'DELETED'`; the non-tx `Count` reads the current-state `entities` table, which is not the snapshot. — §4.3 — name the base query.
[MINOR] §4.5's in-tx RYW branch: survivors from `adds` (the tx's own `tx.Buffer` entries) must be copied before return or the caller aliases live buffer state; today `getAllSnapshotUnlocked` copies committed rows and `buildSnapshot` copies buffer rows. — §4.5 — say "survivors from both streams are copied".

## 4. Simpler alternative

[MINOR] The rejections are reasoned, not asserted: the `GetPage` rung is rejected on snapshot grounds (verified — `getPageDirect` is one statement per page on `readDB`, `entity_store.go:1107`); the deprecation-window rejection gives the mechanism it would defeat; three PRs is reasoned in §10. — §2.2, §3.5, §10 — none.
[MINOR] §4.1 does not record the obvious alternative — memory's sequence baseline captured atomically with `SnapshotTime` (`plugins/memory/txmanager.go:249-276`), which avoids any `Begin` wait. Evaluated: it closes the FCW half of the window but not the visibility half on sqlite (a tx's `readDB` cursor can miss commit X's rows while a later `s.db` read in the same tx queues behind the flush and sees them — two snapshots in one tx). The gate is the right choice; write down why the cheaper option fails. — §4.1 — one paragraph.
[MINOR] §4.1's test asks for "a test hook" to pause a commit mid-flush — production-code seam not needed: plugin tests are in-package, so the test can hold `m.commitMu`, bump `m.lastSubmitTime` under `m.mu` to stand in for step 4, start `Begin` on a goroutine, assert it blocks until release, then assert `SnapshotTime` and the conflicting commit's refusal. — §4.1 — replace the hook with the in-package lock.
[MINOR] §4.3 `json_each`: the driver is `ncruces/go-sqlite3 v0.35.2` (JSON1 built in) and the plugin already uses `json_extract` (`entity_store.go:379`), but `json_each` appears nowhere today — it is new SQL surface. — §4.3 — say so and pin it with a plugin test that runs the query against the real driver.

## 5. Testability

[MAJOR] §6 row 3 "translation failure → classified 400 (both codes)". The `INVALID_CONDITION` leg is reachable: `validateConditionAtDepth` ends in `default: return nil` (`operators.go`), so a caller-built condition type clears validation and `ConditionToFilter` rejects it (`condition_filter.go:181`). The `INVALID_FIELD_PATH` leg is not: it needs `ConditionToFilter` to wrap `ErrInvalidFilterPath` for a path `validateConditionPaths` already accepted with the same `spi.ParseFilterPath` grammar (`jsonpath_grammar.go:178`), and `ConditionToFilter` is a package function that cannot be stubbed. Also `ClassifyStoreQueryError` (`service.go:935`) has no `spi.ErrUnknownOperator` arm; §2.1 lists it as classified, but it reaches the §2.1 default. — §6, §2.1 — split the cell: `Search`-level test for `INVALID_CONDITION`; direct `ClassifyStoreQueryError` test for the `ErrInvalidFilterPath` wrap (or waive the leg inline as unreachable through `Search`); drop `ErrUnknownOperator` from the "classified first" list or add the arm.
[MINOR] §4.4's "yield sequence equals the previous materialised sequence": the previous sequence is Go map iteration order (`getAllTx` builds `map[string]*spi.Entity`), i.e. unspecified. — §4.4 — compare as sets, or assert the new stream equals `ORDER BY entity_id`.
[MINOR] Everything else verifies at its seam without mocking: the engine rung through the existing `nonSearcherEntityStore`, every plugin item in-package against the real store. — §6 — none.

## 6. Migration

[MINOR] Rollout order (§8: plugin internals → SPI tag → engine + pin), compatibility window (none, §10 with mechanism), consumer notification (§3.5, cassandra#95), COMPATIBILITY.md and CHANGELOG `### Breaking` are all present. Rollback is not stated. — §8 — add: PR 1 reverts independently; PR 3 reverts by re-pinning `f6863ae`; SPI tags are immutable, a bad tag is superseded, never moved.
[MINOR] §3.3/§7 line references into the commercial checkout are correct at `494a334` (`entity_store.go:1275`, `:1442`, `model_store.go:252`, `grouped_stats.go:90,93`, `iterate_test.go:114-118`); spitest `TenantIsolation/GetPage` is confirmed absent today. — §3.3, §7 — none.

## 7. Self-containment

[MINOR] "the five refusal branches" (§2.2, §3.2) are never listed; there are exactly five: `entity/service.go:835`, `:1228`, `:1538`, `search/service.go:1337`, `grouped_stats_service.go:280-285`. — §2.2 — list them.
[MINOR] §3.3 "about 25 assertion sites" — measured 17 in `spitest/iterable.go` plus 1 in `filter_not.go`. — §3.3 — write 18.
[MINOR] §2.2 cites `sortEntities` without a file; it is `internal/domain/search/ordersort.go:23` delegating to `spi.LessByOrder`, whose terminal tiebreaker is byte-wise `Meta.ID` (`order_compare.go:27-52`) — the claim holds. — §2.2 — cite the file.
[MINOR] §4.2 cites `txcontext.go:19-23,108` without a module; it is the SPI's `txcontext.go`, not a plugin file. — §4.2 — qualify.
[MINOR] §4.4 names `spi.MergeOrdered` but not the `specs` it must be given; canonical order needs `[]OrderSpec{{Source: SourceMeta, Path: "id"}}` on both `Iterate` (in-tx, where `OrderBy` is rejected) and `GetPage`. — §4.4 — state the specs.
[MINOR] Test-migration counts (§8: 70 lines, 17 files) are close to what a grep shows (67 lines, 19 files including `ModelStore` false positives); good enough to plan from. — §8 — none.

VERDICT: 0 critical, 3 major
